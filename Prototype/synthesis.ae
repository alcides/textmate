# Select the synthesis row below, then Quick Fix… > Synthesize with SMT.
# Aeon must find an expression matching these examples.
@example(double 3 = 6)
@example(double 4 = 8)
def double (n:Int) : Int := ?hole;
